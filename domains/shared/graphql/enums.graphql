# Shared Enum Types
# These enums are used across multiple domains

enum UserRole {
  DEVELOPER
  ORG_ADMIN
  MANAGER
  CONSULTANT
  VIEWER
}

enum PayrollStatus {
  DRAFT
  PENDING_APPROVAL
  APPROVED
  PROCESSING
  COMPLETED
  FAILED
}

enum AuditEventType {
  LOGIN
  LOGOUT
  PERMISSION_GRANTED
  PERMISSION_DENIED
  DATA_ACCESS
  DATA_MODIFICATION
  SECURITY_VIOLATION
}

enum SecurityLevel {
  CRITICAL
  HIGH
  MEDIUM
  LOW
}