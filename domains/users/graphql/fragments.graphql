# User domain fragments - minimal version based on schema

fragment UserFragment on users {
  id
  name
  username
  role
  is_active
  is_staff
  manager_id
  created_at
  updated_at
}

fragment UserWithManagerFragment on users {
  ...UserFragment
  manager {
    id
    name
    is_staff
  }
}