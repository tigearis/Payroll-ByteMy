# Billing Domain Subscriptions - TEMPORARILY DISABLED FOR SCHEMA FIXES
# Security Classification: HIGH - Financial and billing data
# SOC2 Compliance: Real-time access controls with audit logging

# Placeholder subscription to keep file valid during schema fixes
subscription PlaceholderBillingSubscription {
  billingInvoice(limit: 1) {
    id
  }
}