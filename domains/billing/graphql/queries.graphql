# Billing Domain Queries - TEMPORARILY DISABLED FOR SCHEMA FIXES
# Security Classification: HIGH - Financial and billing data
# SOC2 Compliance: Row-level security and audit logging enforced

# Placeholder query to keep file valid during schema fixes
query PlaceholderBillingQuery {
  billingInvoice(limit: 1) {
    id
  }
}