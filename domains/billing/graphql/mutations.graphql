# Billing Domain Mutations - TEMPORARILY DISABLED FOR SCHEMA FIXES
# Security Classification: HIGH - Financial and billing data
# SOC2 Compliance: All mutations logged with comprehensive audit trail

# Placeholder mutation to keep file valid during schema fixes
mutation PlaceholderBillingMutation($id: uuid!) {
  deleteBillingInvoiceById(id: $id) {
    id
  }
}