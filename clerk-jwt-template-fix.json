{
  "name": "hasura",
  "claims": {
    "https://hasura.io/jwt/claims": {
      "x-hasura-allowed-roles": ["{{user.public_metadata.role}}", "viewer", "consultant", "manager", "org_admin", "developer"],
      "x-hasura-default-role": "{{user.public_metadata.role || 'viewer'}}",
      "x-hasura-user-id": "{{user.public_metadata.databaseId || user.id}}",
      "x-hasura-clerk-user-id": "{{user.id}}"
    }
  }
}

INSTRUCTIONS:
1. Copy this JWT template
2. Go to Clerk Dashboard → JWT Templates
3. Find the "hasura" template or create a new one
4. Replace the existing template with this content
5. Save the template
6. The key change: Removed x-hasura-role, now only using x-hasura-default-role