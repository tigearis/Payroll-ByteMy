# Run GraphQL-specific validation (fastest and most important)
echo "🔍 Validating GraphQL operations..."
pnpm lint:graphql

# Skip TypeScript checking for now due to GraphQL schema regeneration
# TODO: Fix TypeScript errors after GraphQL types regeneration
# echo "🔍 Running TypeScript type checking..."
# pnpm type-check

echo "✅ GraphQL validation passed!"