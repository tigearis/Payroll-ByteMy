table:
  name: validation_services
  schema: public
array_relationships:
  - name: geographicPerformances
    using:
      foreign_key_constraint_on:
        column: service_id
        table:
          name: geographic_performance
          schema: public
  - name: rateLimits
    using:
      foreign_key_constraint_on:
        column: service_id
        table:
          name: rate_limits
          schema: public
  - name: serviceAnalytics
    using:
      foreign_key_constraint_on:
        column: service_id
        table:
          name: service_analytics
          schema: public
  - name: serviceApiKeys
    using:
      foreign_key_constraint_on:
        column: service_id
        table:
          name: service_api_keys
          schema: public
  - name: validationResults
    using:
      foreign_key_constraint_on:
        column: service_id
        table:
          name: validation_results
          schema: public
