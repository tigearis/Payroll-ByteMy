table:
  name: resources
  schema: public
array_relationships:
  - name: permissions
    using:
      foreign_key_constraint_on:
        column: resource_id
        table:
          name: permissions
          schema: public
