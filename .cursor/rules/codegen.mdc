---
description: 
globs: 
alwaysApply: false
---
# 🧬 GraphQL Codegen Rules

## Rule Type: Always

- Codegen must be used to generate TypeScript types and Apollo hooks.
- Codegen should run via CI and as a local dev command.
- Fragments, queries, and mutations must all be generated.
- Manual changes to `shared/types/generated/` are prohibited.
