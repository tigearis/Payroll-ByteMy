---
description: 
globs: 
alwaysApply: false
---
# 🚀 Apollo Client Rules

## Rule Type: Always

- Apollo Client is the sole GraphQL client.
- Only use `useQuery`, `useMutation`, `useSubscription` generated by Codegen.
- Apollo Client must be initialised in `lib/apollo-client.ts` and support SSR.
- Avoid using `fetch()`, `graphql-request`, or urql.
- `fetchPolicy` defaults to `cache-and-network` unless explicitly overridden.
